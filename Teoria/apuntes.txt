
  _______    ___________      ______
 |ROUTERS|  |CONTROLLERS|    |MODELS|
 |_______|  |___________|    |______|
 
 -->Router1
 
 const theRouter = express.Router();
 const theController = require("./controller1");
 theRouter.get("-la ruta que sea-", theController.fc1);
 //asi definimos todas las rutas del ruter, nada de funciones anonimas
 
 module.exports = theRouter;
 
 -->Controller1 
 //es el que tiene contacto con el modelo
 const theModel = require("./model1);
 
 function fc1(request, response);
 ... definir todas las funciones del controlador..
 
 //exportamos las funciones
 module.exports = {fc1, fc2,..};
 
 -->Model1
 class model1{
	construct(pool);
	metodo1, metodo2... etc
 }
 
 module.exports = model1;
 
 La aplicación principal quedaría de la siguiente forma:
 
 require .. todos lo que utilizamos hasta ahora más el de los routers
 app=express();
 [set ...]
 [use ... de los middlewares generales]
 //montamos el router de la siguiente forma
 [app.use("/r1", router1); //y asi todos nuestros routers, por pares]
 //por ultimo
 [404]
 [500]
 [listen]
 